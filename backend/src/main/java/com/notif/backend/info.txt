http://localhost:8080/api/concerts/raw?keyword=metallica&size=3

ConcertControlle nimmt zB die Anfrage oben entgegen, liest die Parameter (hier: keyword=metallica) und ruft TMService auf
TMService hat Logik für Komm. mit TM, nutzt QueryBulder um URL zu erstellen, führt den http-request aus und gibt antwort zurück
TMQueryBuilder baut aus Parametern &API-Key gültige URL nach TM
Aktuell bekommen wir rohe JSON als Antwort-> next Step: in DTO verpacken

Was funktionieren sollte mit diesem Commit: Wenn ihr die url in Zeile 1 aufruft sollte eine unformatierte JSON rauskommen
Wichtig: Backend muss laufen